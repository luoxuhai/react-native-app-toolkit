# setAppearanceColorScheme

Override the app's color scheme (light / dark / unspecified).

```javascript
import * as Toolkit from 'react-native-app-toolkit';

Toolkit.setAppearanceColorScheme('dark'); // 'dark' | 'light' | null
```
